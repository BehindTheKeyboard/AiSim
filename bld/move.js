class Move {
    static rotateRight(entity, degrees) {
        // Implementation using the IMovable interface
    }
    static rotateLeft(entity, degrees) {
        // Similar implementation
    }
}
export {};
